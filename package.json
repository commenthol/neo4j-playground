{
  "name": "@commenthol/neo4j-playground",
  "version": "0.0.1",
  "private": true,
  "description": "",
  "keywords": [],
  "license": "MIT",
  "author": "",
  "main": "src",
  "scripts": {
    "all": "npm run clean && npm run lint && npm test",
    "clean": "rimraf lib es coverage .nyc_output",
    "clean:all": "rimraf node_modules && npm run clean",
    "lint": "eslint --cache '**/*.js'",
    "test": "mocha"
  },
  "dependencies": {
    "neo4j-driver": "^1.5.3"
  },
  "devDependencies": {
    "eslint": "^4.19.1",
    "eslint-config-standard": "^11.0.0",
    "eslint-plugin-import": "^2.9.0",
    "eslint-plugin-node": "^6.0.1",
    "eslint-plugin-promise": "^3.7.0",
    "eslint-plugin-standard": "^3.0.1",
    "rimraf": "^2.6.2",
    "mocha": "^5.1.0"
  },
  "engines": {
    "node": ">=8"
  }
}
